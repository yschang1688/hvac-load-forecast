# P6 路線圖：三個結論的下一步（2026-09-23）

依 P3／P4 報告的三個結論回推「該做什麼、不該做什麼」。每項先用三大指標
（完成率／token 與算力效率／維護穩定性）自評，不合格的降級成待辦不開工。

| # | 原始提議 | 資料怎麼說 | 裁定 | 本輪狀態 |
|---|---|---|---|---|
| 1 | 建氣象預報 MCP，給 ML 調用 | perfect vs lagged 全落雜訊帶（lgbm_direct 中位數 0.591 vs 0.686，p=0.34）——**預報的價值未經證實**，不是沒有 | **做契約，不做 server**：價值沒量出來之前，先把因果規則寫進契約與介面，資料源等真實案場 | 契約＋`forecast` 模式＋守門測試 ✅；`noisy_forecast` 中間態 ✅；server ❌（見誠實邊界） |
| 2 | 遞迴式多步預測要與氣象預報一起優化 | 遞迴式劣於免費基準線（p=0.003，h24 誤差 2.25×）；**給它完美預報也沒救**（perfect vs lagged 配對差 +0.038，p=0.34） | **不救遞迴式**。它的病在「把自己的預測餵回去」，不在天氣。想要單模型的維運經濟性，測 DirRec 或多輸出，不是修遞迴 | 不開工；改列 P7 候選 |
| 3 | 換掉 PSI，用別的指標當重測標準 | 624/624 週 alert、三種參考期皆同；概念漂移 PSI 上升率 58%（擲硬幣 50%）vs 誤差 77% | **主訊號改誤差，PSI 降級旁證**；門檻事前宣告；重訓後未改善不晉升 | 函式＋排程接口＋守門測試 ✅；離線重算 624 週 ✅；部署模擬重跑 ❌（需數小時） |

## 三、誤差監控：本輪實際量到的東西

`src/analyze_p4_error.py` 用 P4 已落盤的 624 週離線重算，**不重跑模擬**。門檻 1.3× 與基準期長度 8 週皆寫死在檔頂。

| 基準期選法 | 誤報率（注入前） | 偵測率（注入後） | 讀法 |
|---|---|---|---|
| PSI（任何參考期） | 100% | 100% | 恆為 alert＝無資訊 |
| 注入前全部週中位數（P4 §三口徑，in-sample） | 28% | 51% | 報告原數字，可重現 |
| 前 8 週凍結基準（out-of-sample） | **48%** | 67% | 1–2 月是冬季，基準偏低，夏季誤差自然高於它——**季節性也會咬誤差監控** |
| 滾動 8 週基準（lag 1） | 31% | 24%（整段） | 突變後數週被吸收成新常態；**首次 alert 距注入 0／0／0／1／6／9 週**，6 棟中 4 棟一週內抓到 |

三個結論：
1. 誤差監控**有鑑別力**（會說 stable 也會說 alert），PSI 沒有。這是 P4 就下的結論，本輪只是把它做成可呼叫的函式。
2. **固定早期基準會被季節性咬**：這是本輪新發現。正解是「去年同期」基準，但 P4 的部署模擬只有 2017 一年，量不出來——列 P7。
3. 滾動基準抓突變快、抓緩慢漂移弱。生產上兩個一起用（滾動抓突變、去年同期抓緩慢），本輪只做了前者。

## 誠實邊界（這輪沒做的）

- **沒有重跑部署模擬**。上表全是離線重算；「用誤差觸發的重訓比冷卻期排程的重訓好多少」仍未量。重跑 6 棟 × 2 組估 4–6 小時。
- **`noisy_forecast` 的 σ(h) 是佔位值**（乾球 0.8→1.8 °C、露點 1.0→2.2 °C），未引用任何 NWP 驗證數字。跑出來的結果在引用前必須先換成 `get_forecast_skill` 給的值。
- **真實預報回放在本資料集上做不出來**：BDGP2 是 2016–2017，免費預報封存從 2021 起。這條線要等真實案場。
- **MCP server 未實作**，只有契約與 ML 端介面。
- `reports/p4_drift.csv` 的 `max_psi` 中位數 7.98，與 P4 報告修正 Laplace 平滑後的 2.94 不一致——CSV 可能是修正前的落盤。不影響「100% alert」的結論，但重跑時要一併確認。

## P7 候選（依三指標排序，未開工）

1. **視窗層級關機門檻事前宣告後重跑 P3 聚合**（P3 待辦 1）——主結論能不能宣稱就看它，成本一次聚合、零訓練。
2. **誤差監控的去年同期基準**——需 2 年部署模擬或改用 2016 訓練期的 out-of-fold 誤差當基準。
3. **DirRec／多輸出當單模型方案**，配 `noisy_forecast` 三種天氣模式一起跑（P3 待辦 2 的延伸）。
4. NN 給足算力再判架構差異（P3 待辦 3）——算力最貴，排最後。
